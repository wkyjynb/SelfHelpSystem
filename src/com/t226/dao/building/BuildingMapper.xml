<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.t226.dao.building.BuildingMapper">

<select id="getBuilding" resultType="Building"><!--根据父id和区域id进行联动查询-->
   SELECT * FROM security_building where parentId=#{parentId} and addressId=#{addressId}
</select>



</mapper>